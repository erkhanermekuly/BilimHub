# BilimHub - Образовательная платформа

## 🎓 Описание проекта

BilimHub - это современная образовательная онлайн-платформа для изучения программирования с системой курсов, лекций, тестирования и рейтингом пользователей.

##  Основной функционал

###  Система аутентификации
- Регистрация и вход пользователей
- JWT токены (HTTP-only cookies)
- Хеширование паролей с bcrypt
- Роли: пользователь и администратор

### 📚 Система обучения
- Темы курсов с лекциями
- Markdown контент лекций
- Видео материалы
- Двухуровневая система тестирования
- Автоматическая проверка тестов
- Прогресс-трекинг пользователей
- Последовательная разблокировка лекций

### Тестирование
- Тест 1: основная попытка (проходной балл 70%)
- Тест 2: автоматически открывается при провале теста 1
- Одиночный и множественный выбор ответов
- Автоматический подсчет баллов

### Рейтинговая система
- Накопление баллов за прохождение тестов
- Уровни: Новичок → Средний → Продвинутый → Эксперт
- Таблица лидеров

### Админ-панель
- Управление темами курсов (создание, редактирование, удаление)
- Управление лекциями (создание, редактирование, удаление)
- Статистика платформы
- Доступ только для администраторов

После входа администратор автоматически перенаправляется в админ-панель по адресу `/admin`


### Шаги установки

1. Клонировать репозиторий:
```bash
git clone https://github.com/erkhanermekuly/BilimHub.git
cd BilimHub
```

2. Установить зависимости:
```bash
npm install
```

3. Настроить базу данных в `.env`:
```env
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=test
DB_DIALECT=postgres
DB_PORT=5432

PORT=5000
NODE_ENV=development

SESSION_SECRET=your-secret-key
JWT_SECRET=your-jwt-secret
```

4. Запустить миграции:
```bash
npm run db:migrate
```

5. Заполнить тестовыми данными:
```bash
npx sequelize-cli db:seed --seed 20251122064009-create-admin-user.js
npx sequelize-cli db:seed --seed 20251120124609-demo-learning-content.js
```

6. Запустить сервер:
```bash
npm start
```

Сервер запустится на http://localhost:5000

## 📝 Структура БД

### Таблицы
- `users` - пользователи (role: user/admin)
- `themes` - темы курсов
- `lectures` - лекции
- `tests` - тесты (2 на каждую лекцию)
- `questions` - вопросы тестов
- `answers` - варианты ответов
- `user_progress` - прогресс пользователей
- `user_ratings` - рейтинг пользователей

## 🎨 Технологии

### Backend
- Node.js + Express 5.1.0
- PostgreSQL + Sequelize ORM
- JWT + bcrypt
- express-session

### Frontend
- EJS шаблоны
- Vanilla JavaScript
- CSS3 (отдельные файлы стилей)
- Marked.js (Markdown парсер)

## 📱 Страницы

- `/` - Главная страница
- `/login` - Вход
- `/register` - Регистрация
- `/profile` - Профиль пользователя
- `/themes` - Список курсов
- `/themes/:id` - Лекции темы
- `/lectures/:id` - Просмотр лекции с тестами
- `/rating` - Рейтинг учеников
- `/contacts` - Контакты
- `/admin` - Админ-панель (только для администраторов)

## 🔧 API Endpoints

### Публичные
- `GET /api/themes` - список тем
- `GET /api/themes/:id` - детали темы
- `GET /api/ratings` - рейтинг пользователей

### Для авторизованных
- `GET /api/lectures/:id` - получить лекцию
- `GET /api/tests/:id` - получить тест
- `POST /api/tests/:id/submit` - отправить ответы

### Для администраторов
- `POST /api/themes` - создать тему
- `PUT /api/themes/:id` - обновить тему
- `DELETE /api/themes/:id` - удалить тему
- `POST /api/themes/:themeId/lectures` - создать лекцию
- `PUT /api/lectures/:id` - обновить лекцию
- `DELETE /api/lectures/:id` - удалить лекцию

## 🎯 Особенности

1. **Автоматическая разблокировка**: Лекции открываются последовательно после прохождения предыдущих
2. **Двойное тестирование**: Вторая попытка при провале первого теста
3. **Умный подсчет**: Автоматический подсчет баллов и обновление рейтинга
4. **Безопасность**: JWT токены, роли, защищенные маршруты
5. **Адаптивный дизайн**: Современный UI с градиентами и анимациями

